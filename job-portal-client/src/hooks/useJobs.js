import { useEffect, useState } from "react";import { jobsApi } from "../api/jobsApi";export const useJobs = () => {  const [jobs, setJobs] = useState([]);  const [isLoading, setIsLoading] = useState(true);  const [error, setError] = useState(null);  useEffect(() => {    const fetchJobs = async () => {      try {        setIsLoading(true);        const data = await jobsApi.getAll();        setJobs(data);      } catch (err) {        setError(err.message || "Failed to load jobs");      } finally {        setIsLoading(false);      }    };    fetchJobs();  }, []);  return { jobs, setJobs, isLoading, error };};