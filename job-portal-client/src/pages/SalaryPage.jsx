import React, { useEffect, useMemo, useState } from "react";import PageHeader from "../components/PageHeader";const SalaryPage = () => {  const [searchText, setSearchText] = useState("");  const [salary, setSalary] = useState([]);  useEffect(() => {    fetch("salary.json")      .then((res) => res.json())      .then((data) => setSalary(data));  }, []);  const filteredSalary = useMemo(() => {    if (!searchText.trim()) return salary;    return salary.filter((job) => job.title.toLowerCase().includes(searchText.toLowerCase()));  }, [salary, searchText]);  return (    <div className="max-w-screen container mx-auto xl:px-24 px-4">      <PageHeader title={"Estimate Salary"} path={"Salary"} />      <div className="mt-5">        <div className="search-box p-2 text-center mb-2">          <input            type="text"            id="search"            name="search"            placeholder="Type Here to Search"            className="py-2 pl-3 border focus-within:ring-indigo-600 lg:w-6/12 mb-4 w-full"            onChange={(e) => setSearchText(e.target.value)}            style={{ outlineColor: "#4F46E5" }}          />        </div>      </div>      <div className="grid lg:grid-cols-3 sm:grid-cols-2 grid-cols-1 gap-12 my-12 items-center">        {filteredSalary.map((data) => (          <div key={data.id} className="shadow px-4 py-8">            <h4 className="font-semibold text-xl">{data.title}</h4>            <p className="my-2 font-medium text-blue text-lg">{data.salary}</p>            <div className="flex flex-wrap gap-4">              <a href="/" className="font-bold">                {data.status}              </a>              <a href="/" className="font-bold">                {data.skills}              </a>            </div>          </div>        ))}      </div>    </div>  );};export default SalaryPage;