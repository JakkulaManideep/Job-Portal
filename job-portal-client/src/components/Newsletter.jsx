import React, { useState } from "react";import { FaEnvelopeOpenText, FaRocket } from "react-icons/fa6";import Swal from "sweetalert2";const Newsletter = () => {  const [email, setEmail] = useState("");  const handleSubscribe = async (e) => {    e.preventDefault();    if (!email.trim()) {      await Swal.fire({        icon: "error",        title: "Error",        text: "Please enter your email address.",      });      return;    }    setEmail("");    await Swal.fire({      icon: "success",      title: "Success!",      text: "You have been subscribed to our newsletter!",    });  };  const handleResumeUpload = async (event) => {    const selectedFile = event.target.files?.[0];    if (!selectedFile) return;    await Swal.fire({      icon: "success",      title: "Success!",      text: "Your resume has been uploaded successfully!",    });  };  return (    <div>      <div>        <h3 className="text-lg font-bold mb-2 flex items-center gap-2">          <FaEnvelopeOpenText /> Email Latest Jobs        </h3>        <p className="text-primary/75 text-base mb-4">          Subscribe to our newsletter for latest job updates.        </p>        <div className="w-full space-y-4">          <input            type="email"            id="email"            name="email"            placeholder="Email"            required            value={email}            onChange={(e) => setEmail(e.target.value)}            className="w-full block py-2 pl-3 border focus:outline-none"          />          <input            type="submit"            value="Subscribe"            required            onClick={handleSubscribe}            className="w-full block py-2 pl-3 border focus:outline-none bg-blue rounded-sm text-white cursor-pointer font-semibold"          />        </div>      </div>      <div className="mt-20">        <h3 className="text-lg font-bold mb-2 flex items-center gap-2">          <FaRocket /> Get Noticed Faster        </h3>        <p className="text-primary/75 text-base mb-4">          Upload your resume here to get noticed faster by top companies.        </p>        <div className="w-full space-y-4">          <input            type="file"            id="resumeInput"            accept=".pdf,.doc,.docx"            onChange={handleResumeUpload}            className="w-full block py-2 pl-3 border focus:outline-none bg-blue rounded-sm text-white cursor-pointer font-semibold"          />        </div>      </div>    </div>  );};export default Newsletter;